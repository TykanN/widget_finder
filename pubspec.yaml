name: widget_finder
description: widget_finder makes it easy to find the position and size of the widget.
version: 0.1.1
homepage: https://github.com/TykanN/widget_finder

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
